---
trigger: always_on
---
# UI 风格一致性规范

每次新增 UI 样式或新页面时，必须按照现有项目的整体风格进行设计，确保与项目已有的视觉语言保持一致。具体要求如下：

- **色彩体系**：沿用项目中已定义的主题色、辅助色和背景色，不随意引入未使用的颜色。
- **字体与排版**：保持与现有页面一致的字体大小、行高、字重和间距规范。
- **组件风格**：按钮、输入框、卡片、弹窗等组件应与已有组件的圆角、阴影、边框等样式统一。
- **布局结构**：新页面的布局方式（如间距、对齐、栅格系统）需与现有页面保持协调。
- **交互反馈**：动画效果、过渡时间、hover/active 状态等交互细节应与项目整体风格匹配。
- **图标与图片**：使用与项目一致的图标风格（线性/填充/双色）和图片处理方式。

> ⚠️ 在开发前应先参考项目中已有的页面和样式文件，充分理解现有设计语言后再进行开发，避免出现风格突兀或不一致的情况。

## 部署流程

在完成代码修改或新增功能后，**不要自动执行部署操作**。请保持当前工作区状态，等待用户自行核对并执行部署命令。
> 用户会自行负责部署，AI 助手不应主动调用部署命令或推送变更。
