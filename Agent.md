---
trigger: always_on
---
# HappyChat 协作规则（常驻摘要）

> 完整项目规范见 **CLAUDE.md**（接口/WS 协议、命名、数据模型、验证要求），冲突时以 CLAUDE.md 为准。

## UI 风格一致性

每次新增 UI 样式或新页面时，必须沿用项目现有视觉语言（Warm Mist 设计系统）：

- **色彩**：只使用 `App.vue :root` 中的设计令牌（主色 #FF6B6B、辅色 #A78BFA、主渐变、背景 #FAF9F7 等），不随意引入新颜色。
- **字体与排版**：与现有页面保持一致的字号（正文 28-30rpx）、行高、字重（500/600/700）和间距。
- **组件**：按钮、输入框、卡片、弹窗复用 GradientButton / FormInput / BaseModal / StateView / AppAvatar，圆角、阴影、边框与现有组件统一。
- **布局与交互**：间距对齐方式、入场动画（fadeIn/slideUp）、按压反馈（:active 缩放 0.94-0.98）、过渡时长（0.15-0.3s）与整体风格匹配。
- **图标与图片**：统一 uni-icons 线性图标；头像无图时走 format.js 渐变色板。

> ⚠️ 开发前先阅读现有页面（chat/list、profile 等）理解设计语言，避免风格突兀。

## 部署纪律（硬性约束）

完成代码修改后**不要自动执行部署**，严禁 `git push`、严禁改动远程服务器。保持工作区现状，由用户自行核对并执行部署命令。
